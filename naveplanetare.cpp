# include <fstream>
# include <vector>
# include <algorithm>
# define NR 1000005
# define O 1200
using namespace std;
ifstream f("naveplanare.in");
ofstream g("naveplanare.out");
int i,j,n,m,K;
int d
int main ()
{
    f>>n>>K;
    for (i=1; i<=n; ++i)
    {
        f>>a[i].x>>a[i].y;
        ++linie[a[i].x]; ++coloana[a[i].y];

    }

    back (int k)

    g<<maxx<<"\n";

    return 0;
}
