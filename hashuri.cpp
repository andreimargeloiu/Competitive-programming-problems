# include <bits/stdc++.h>
# include <unordered_map>
using namespace std;
ifstream f("hashuri.in");
ofstream g("hashuri.out");
unordered_map <char, vector<int>> H;
int i,j,n,m,tip,x;
int main ()
{
    H["ada"].push_back(5);
    H["ada"].push_back(5);
    H["ada"].push_back(7);

    std::unordered_map <char, vector<int>>::iterator it;

    /*for (auto it: H["ada"]) {
        g<<x<<" ";
    }*/

    return 0;
}
